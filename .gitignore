/target
*.swp
.env
*.log

# Claude Code project-local MCP config. Contains paths and credentials
# specific to the local checkout; each developer constructs their own.
.mcp.json

# Local project tracking database (SQLite WAL companions included).
/project.sqlite3
/project.sqlite3-*

# Python verifier/test bytecode caches.
__pycache__/
*.py[cod]
